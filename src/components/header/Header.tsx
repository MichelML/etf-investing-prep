import * as React from "react";

const Header = (props = {}): JSX.Element => (
    <header className="column bg-blue center-xs">
        <h4 className="col-xs-12 white">ETFs Weighted Investing</h4>
    </header>
);

export default Header;
